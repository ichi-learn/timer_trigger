name: Deploy Python Function App to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v4

      - name: 'Setup Python version'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 'Install dependencies'
        shell: bash
        run: |
          # 依存関係を専用フォルダにインストール（配置ミスを防ぐ定石）
          python -m pip install --upgrade pip
          pip install -r requirements.txt --target=".python_packages"

      - name: 'Login to Azure'
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 'Deploy to Azure Functions'
        uses: Azure/functions-action@v1
        with:
          app-name: 'ichi-translator-rebuilt-20240523'
          # 「今いる場所（リポジトリルート）」を指定
          package: .
          # ここが魔法のスイッチ：Azure側でZip展開と構成をやり直させます
          scm-do-build-during-deployment: true
          enable-oryx-build: true

      - name: 'Set Environment Variables via CLI'
        # 念のため、デプロイのたびに「V2認識フラグ」を強制的に上書き設定します
        run: |
          az functionapp config appsettings set \
            --name ichi-translator-rebuilt-20240523 \
            --resource-group ichi-learn-rg-rebuilt \
            --settings AzureWebJobsFeatureFlags=EnableWorkerIndexing WEBSITE_RUN_FROM_PACKAGE=1